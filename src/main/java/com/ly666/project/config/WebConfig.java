package com.ly666.project.config;import org.springframework.context.annotation.Configuration;import org.springframework.web.servlet.config.annotation.CorsRegistry;import org.springframework.web.servlet.config.annotation.InterceptorRegistry;import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;/** * @author ：i18n * @date ：Created in 2019/4/21 22:39 * @desc : {} */@Configurationpublic class WebConfig implements WebMvcConfigurer {	//配置拦截器	public void addInterceptors(InterceptorRegistry registry) {		//registry.addInterceptor(new AuthInterceptor()).addPathPatterns("/blogServer/main/**");	}	//配置跨域请求	public void addCorsMappings(CorsRegistry registry) {		registry.addMapping("/**")				.allowedOrigins("http://localhost:8080")     // 允许指定域名访问				.allowedHeaders("*")                         // 允许任何的head头部				.allowedMethods("*")                         // 允许任何的请求方法				.maxAge(600)                                 //预检请求的结果缓存10分钟				.allowCredentials(true);	}}